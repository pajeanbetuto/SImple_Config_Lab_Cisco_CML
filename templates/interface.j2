{% for interface in interfaces %}
interface {{ interface.name }}
 ip address {{ interface.ip_address }} {{ interface.mask }}
 description {{ interface.description }}
 {% if interface.active or interface.description == "mgt_interface" %}
  no shutdown
 {% else %}
  shutdown
 {% endif %}
{% endfor %}
